next-version: 1.0.0
mode: ContinuousDeployment

branches:
  master:
    regex: '^master$'
    increment: patch

  develop:
    regex: '^develop$'
    increment: minor

  hotfix:
    regex: '^hotfix[/-]'
    increment: patch
    source-branches: ['master']

